# CRA Skill 通用产品约束

状态：已确认  
确认日期：2026-07-23

## 1. 适用范围

本约束适用于本项目沉淀的所有 CRA Skill。首阶段目标是基于一线 CRA 的工作经验跑通可复用工作流；完整法域适配在 Skill 接触真实项目数据或进入正式业务使用前完成。

本项目采用以下业务边界：

- CRA 只能在研究中心允许的场景中阅读受试者源数据。
- Agent 不进入受试者源数据域。
- Agent 只处理 CRA 获准放入本地项目工作区的项目文件。
- 项目文件原则上不包含受试者直接识别信息。
- 筛选号、随机号等编码化标识不属于直接识别信息，但相关文件仍按受限的临床试验数据管理。

## 2. 责任划分

### 2.1 Skill 必须强制保证

- 不访问、复制、拍摄、转录或缓存受试者源数据。
- 只访问 CRA 为本次任务明确指定的项目工作区。
- 不处理姓名、身份证号、联系方式、住址、病历号等受试者直接识别信息。
- 项目输入文件默认只读，输出保存至独立目录。
- 输出默认标记为草稿，不自动成为正式项目记录。
- 不自动发送、上传、提交、签署、删除或覆盖正式文件。
- 记录输入、输出、规则版本、执行时间和人工确认结果。
- 发现疑似源数据、直接识别信息、权限不明或规则冲突时停止处理。
- 明确提示是否调用在线模型、联网工具或外部连接器。
- 未经组织或项目授权，不向在线服务提供编码化试验数据。

### 2.2 CRA 必须负责

- 确认输入文件属于获准管理的项目文件。
- 确认输入文件不含源数据和受试者直接识别信息。
- 确认申办方、CRO、中心及当前项目允许使用 Agent。
- 审核输出的事实准确性、完整性和适用性。
- 完成医学、方案、偏差、安全性及合规判断。
- 执行最终签名、发送、提交和正式归档。

## 3. 通用约束

### G-01 工作区限制

Skill 只能访问用户本次明确指定的项目目录，不得扫描其他磁盘、个人目录、相邻项目或未授权位置。

### G-02 源数据禁入

Skill 不得读取或处理受试者源数据、源文件截图、病历复制件及其人工转录内容。即使 CRA 当时位于研究中心，也不得把源数据提供给 Agent。

### G-03 直接标识禁入

不得输入受试者姓名、身份证号、联系方式、住址、病历号等直接识别信息。

### G-04 编码数据受限

筛选号、随机号等编码化数据仅在完成工作确有必要且处理环境已经获准时使用，不得默认提供给在线服务。

### G-05 最小必要

每个 Skill 只能读取完成当前任务所必需的文件和字段。

### G-06 输入只读

Skill 不得修改、移动、重命名或删除项目输入文件。

### G-07 输出隔离

输出必须保存到指定目录，并保留来源、生成时间和版本信息。

### G-08 草稿原则

Agent 输出默认是工作草稿。未经 CRA 审核，不得视为正式记录、最终结论或已完成事项。

### G-09 人工审批

发送、提交、上传、签署、覆盖及正式归档必须由 CRA 明确确认。

### G-10 专业判断保留

Skill 可以整理证据、发现差异和提出建议，但不得代替医学、安全性、方案偏差或最终合规判断。

### G-11 可追溯性

执行记录必须包含输入文件、使用的规则或模板版本、输出文件和确认结果，但不得在日志中复制受限数据正文。

### G-12 异常停止

发现疑似源数据、直接识别信息、权限不明或规则冲突时，Skill 必须停止并提示人工处理。

### G-13 正式系统优先

EDC、CTMS、eTMF 及获批项目系统仍是正式记录来源，本地输出不能自行取代这些系统。

### G-14 组织规则优先

申办方、CRO、研究中心和具体项目的授权范围高于通用 Skill。CRA 个人确认不能替代组织授权。

## 4. 首阶段运行限制

在完整环境与合规适配完成前，CRA Skill 应遵守以下限制：

- 优先使用模拟数据、脱敏数据、空白模板或不含受试者信息的项目文件。
- 只生成本地草稿和检查结果。
- 不连接生产系统执行写入。
- 不自动外发信息。
- 不自动签署或提交正式记录。

## 5. 外部知识库接入边界

本项目只负责 CRA Skill 的沉淀，不负责建设或维护项目知识库。项目知识库是独立项目，也是 CRA Skill 的可选外部依赖。

所有 CRA Skill 必须满足：

- 在没有知识库的情况下，可以读取项目工作区中的规范化项目信息文件并独立运行。
- 可以通过稳定的知识库契约读取外部知识库中已经确认的项目或中心事实。
- 不依赖 Obsidian 的具体笔记名称、目录结构、排版或插件。
- 知识库不可用时，可以回退到规范化项目信息文件；事实缺失或冲突时请求 CRA 确认。
- 不直接修改外部知识库，只能输出候选事实更新包，交由知识库项目审核和导入。
- 不把知识库接入作为安装、运行或复用 CRA Skill 的必要条件。

### 5.1 多格式提取责任

多格式项目资料的 OCR、邮件及附件解析、事实归一化、版本治理和人工确认属于独立知识库项目，不属于结构化表单填写 Skill。

结构化表单填写 Skill 只负责：

- 从规范化项目信息文件或知识库契约读取统一项目事实。
- 识别目标表单类型和模板配置。
- 将统一项目事实映射到目标字段。
- 填写一个 `.xlsx` 或 `.docx` 目标文件。
- 输出缺失、冲突和来源记录。

当前原型使用规范化项目信息文件模拟知识库未来提供的统一事实接口。

知识库契约至少应支持以下能力：

```text
get_project_fact(key, scope)
get_site_fact(site_id, key)
search_project_documents(query, scope)
```

返回结果至少包含：

```yaml
value: ABC-2026-001
scope: project
status: confirmed
source: Protocol_V2.0.pdf
source_location: Cover Page
effective_date: 2026-05-01
```

## 6. 结构化表单 Skill 范围

- 支持多种结构化表单类型。
- 每次只填写一个目标文件，不支持批量填写。
- 目标文件格式仅为 `.xlsx` 和 `.docx`。
- 每种具体版式通过模板配置适配，不要求在开发前收集全部未来模板。
- 首个真实模板用于跑通闭环，第二个结构不同的模板用于验证通用性。
- 信函与沟通文件、监查报告附件、监查报告初稿按后续独立 Skill 依次开发。
